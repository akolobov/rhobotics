
pretrained_checkpoint: "/datadisk/dean/models/training_phi4mm_aloha_bb/0209_210526_eef/checkpoints/checkpoint_step_0050000.pt"

environment:
  type: "aloha_server"
  port: 7000

policy_interface_cfg:
  observation_mapping:
    joint_position: observation.state
    cam_high: observation.image.0
    cam_left_wrist: observation.image.1
    cam_right_wrist: observation.image.2
    task: task

  # eval_mode: rtc
  # inference_delay: 8
  # beta: 10
