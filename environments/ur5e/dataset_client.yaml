# UR5e dataset client configuration
# Maps server observation keys to LeRobot dataset keys
obs_mapping:
  joint_positions: observations.joint_position
  zed_scene_bgr: observations.images.cam_scene
  zed_wrist_left_bgr: observations.images.cam_left_wrist
  zed_wrist_right_bgr: observations.images.cam_right_wrist
  actual_tcp_forces: observations.tcp_forces
  tactile: observations.tactile
  task: task

gt_action_key: action.joint_position
