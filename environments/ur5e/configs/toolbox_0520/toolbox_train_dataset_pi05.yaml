# Training split for the UR5e bimanual toolbox task -- Pi0.5 variant.
# Identical to toolbox_train_dataset.yaml except it points at transforms_pi05.yaml
# (no color_jitter -- Pi0 applies its own internally).
repo_id: "msr_custom"
root_dir: "/data/msrx-eai/datasets/ur5e/ur5e_toolbox/ur5e_toolbox_0520"

observation_mapping: !include observation_mapping.yaml
transform_mapping: !include transforms_pi05.yaml

<<: !include toolbox_train_features.yaml
